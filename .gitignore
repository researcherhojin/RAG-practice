# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# .env 추가
.env

# RAG 산출물 / Vector DB (GitHub 에 올리지 않음)
chroma_db/
outputs/
.gstack/

# tiktoken 인코더 캐시 (한 번 받으면 재생성 가능 — git 제외)
.tiktoken_cache/

# 문서 파일명이 담긴 평가 질문 (개인정보 보호 — git 제외)
# 단, docs/images/ 스크린샷은 공개 샘플 문서 기준이라 커밋해 README 에 노출함.
eval/questions.yaml

# 추가 비밀/인증 파일 (방어용)
.env.*
*.key
*.pem
